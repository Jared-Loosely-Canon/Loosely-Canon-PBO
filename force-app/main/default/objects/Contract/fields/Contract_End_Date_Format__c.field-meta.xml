<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Contract_End_Date_Format__c</fullName>
    <description>Used to format the date to MM/DD/YYYY outside of the UI</description>
    <externalId>false</externalId>
    <formula>IF( ISBLANK(EndDate), &apos;&apos;, 
TEXT(MONTH( EndDate )) + &quot;/&quot; + TEXT(DAY(EndDate )) + &quot;/&quot; + TEXT(YEAR(EndDate ))
)</formula>
    <inlineHelpText>Used to format the date to MM/DD/YYYY outside of the UI</inlineHelpText>
    <label>Contract End Date Format</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
